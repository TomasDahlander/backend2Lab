apiVersion : v1
kind: Service
metadata:
  name: bank-service
  namespace: labenv
spec:
  selector:
    app: bank
  ports:
    - name: application
      protocol: TCP
      port: 8080
      targetPort : 8080
  type: ClusterIP