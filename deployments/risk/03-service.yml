apiVersion : v1
kind: Service
metadata:
  name: risk-service
  namespace: labenv
spec:
  selector:
    app: risk
  ports:
    - name: application
      protocol: TCP
      port: 8082
      targetPort : 8082
  type: ClusterIP
