version: "3.1"

services:

  mysql:
    container_name: mysql-bank
    image: mysql:latest
    hostname: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: bank
    ports:
      - 3309:3306

  java-app:
    container_name: bank-app
    image: dalle90/bank:2.0.0
    ports:
      - 8081:8080
    environment:
      MYSQL_HOST: mysql
    depends_on:
      - mysql
